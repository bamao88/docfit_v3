只在拿到真实页图时判定 section/页眉脚/页码/版式；拿不到页图就 abstain，不要猜。每条 section 必须绑定一个输入中已有的 T1 section source_ref，边界必须覆盖有效 source_seq，并带 page_no + render_target_id 的 evidence_refs。声明 no_page_field 时必须列出实际检查过的正文范围和页眉页脚 part。T4 不判断单元起止，也不输出单元分页策略。
