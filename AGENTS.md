# AGENTS.md

DocFit v3 是一个把学生论文转换成学校模板格式的产品。

本文件只写 agent 在本仓库工作的高层原则、进入方式和常见错误。具体文档格式、命名、frontmatter、状态流转、索引维护等操作细节，进入对应目录后先读该目录说明，例如 `docs/plans/README.md`。

## 1. 基本工作方式

做任何任务前，先回答四个问题：

- 本轮用户真正要达成的目标能力是什么？
- 当前事实源在哪里：代码、issue、plan、标准、真实运行产物，还是用户最新描述？
- 完成信号是什么：哪些测试、真实样本、扫描、裁判报告或输出契约能证明目标成立？
- 工作区现在有什么未提交改动，哪些属于本轮，哪些必须避开？

不要先写代码再反推目标。目标不清楚时，先查事实源；事实源冲突时，指出冲突并按最新用户目标和仓库规则重新对齐。

## 2. 交付原则

- 默认使用中文讨论、计划、总结和最终回复，除非用户明确要求其他语言。
- 以能力为单位交付，不以当前暴露的单个失败样本为单位修补。
- 不把系统性目标降级成局部样本补丁、窄规则或“看起来能过”的最小修复。
- 诊断优先于过关。评测、裁判、日志和报告首先要解释“哪里不一致、为什么不一致、该谁修、怎么验”。
- 验收必须能证明目标能力成立。按风险选择单测、契约测试、真实运行、代表性扫描、反例验证和残留检查。
- 不用 `PASS`、`SIGNABLE`、artifact 存在、单个 replay 成功或少量单测通过替代真正完成。
- 保护工作区。修改前看 `git status`；不要回滚、整理、提交与本轮目标无关的用户或其他 agent 改动。

## 3. 分层责任

保持上游事实、下游判断、执行动作和裁判诊断的边界清楚。

- 上游事实层只记录事实，不提前写下游语义判断。
- 识别/规划层基于事实做结构判断，并保留证据来源。
- 执行层只执行已经明确的动作契约，不偷偷补语义推断。
- 裁判层负责差异诊断、owner 归因和修复建议，不用 gate 结果遮盖问题。

模板生成中的 T1 边界是典型例子：T1 `document_facts` 只能输出源 DOCX 事实，例如文本、OOXML 位置、run、样式、字段、分页/分节、表格、页眉页脚和可见对象 trace；不输出 `unit_id`、`policy`、`confidence`、`is_toc_entry` 等下游判断。

## 4. 做不同事情时怎么进入

### 修改代码

先读相关模块、测试和已有产物，再确定实现点。优先沿用现有模型、数据结构、命名和测试风格。改动完成后，用能证明目标能力的最小充分测试集验证；风险高或跨阶段时，补真实样本或端到端验证。

同时控制文件职责和长度：不要因为改动方便就持续往同一个文件追加代码。新增逻辑前先判断是否形成了独立职责；如果一个文件同时承担编排、领域判断、数据转换、外部调用等多类工作，或已经明显难以阅读和测试，应按职责拆成命名清楚的小模块，并保持依赖方向清晰。拆分不是把长代码机械搬到多个文件，而是让每个文件有单一、可解释的用途。

提示词不要与业务流程、工具调用或结果解析代码混写。较长提示词、可复用提示片段和输出格式说明应放进独立的 prompt 模块或资源文件，由代码通过明确接口加载；业务代码只保留选择提示词、传入变量和处理结果的逻辑。修改提示词时同步检查对应测试、版本或契约，避免提示词变化悄悄改变下游行为。

### 修 bug

先复现或定位证据，再判断根因。不要只根据一个失败样本加启发式。修复后要加能覆盖根因的测试，并检查是否存在同类残留。

### 写 issue / plan / 阶段文档

先进入 `docs/plans/` 读取 README、索引和相关历史文档。issue 记录问题事实，plan 承载执行契约；不要为同一轮工作再创建第二套 execution plan。若 plan 不够可执行，更新原 plan；若目标或根因变化，进入下一轮 issue/plan。

### 做验收或裁判

先区分“运行成功”和“质量通过”。报告应明确 expected vs observed、mismatch、root cause、owner、fix plan 和残留。不要用总状态掩盖阶段差异。

### 提交 Git

提交只包含本轮直接相关改动。提交信息或正文应能追溯对应 issue/plan，并记录关键验证证据和已知残留。未完全闭环时，不要把状态写成完成。

## 5. 常见错误

- 只修当前样本，不回到目标能力。
- 只传递字段或写 artifact，但没有下游消费和验收。
- 把 AI observation、代码规则、merged 产物混在一起，导致无法判断谁生效。
- 用单测通过、`PASS`、`SIGNABLE` 或某次 replay 成功宣称完成。
- plan 太上层时另写一份执行文档，制造第二事实源。
- 修改文档索引或状态时不读对应 README。
- 在 dirty worktree 中误提交、误回滚或格式化无关文件。
- 发现残留后只在最终回复里说明，没有回写 issue/plan/index 或后续追踪点。

## 6. 沟通方式

用户不是以技术实现细节为主要背景来理解项目。沟通时先讲结论、业务含义和下一步，再补必要证据；不要一上来堆代码名、函数名、测试名或内部术语。

- 先回答“这件事现在能不能用、影响什么、还差什么”，再解释“技术上为什么”。
- 使用产品和流程语言，例如“最终 Word 里还会残留格式说明”，优先于“某个 parser 没覆盖某个 run span”。
- 必须提技术名词时，顺手解释它在产品里的作用，例如“route-eval，也就是用来比较不同生成路线哪里不一致的裁判报告”。
- 区分三类状态：已经完成、部分完成但有残留、还没开始或没闭环。不要用含糊的“基本完成”代替。
- 给用户看的总结保持短而清楚；复杂证据可以放在后面，或者只列最关键的文件、命令和结果。
- 如果计划和现实不一致，直接说明 gap、风险和建议下一步，不把问题包装成技术细节。
